package org.example;
/*Escribir un programa que solicite al usuario dos vectores de Nelementos y que imprima su producto escalar. Utilice un 
array para representar el vector de N elementos. Realice la entrada de usuario porteclado en una función que reciba las 
dimensiones del vector a la que seinvoca una vez (los vectores deben tener el mismo número de dimensiones)o dos veces y se
establezcan las dimensiones no establecidas a 0. Crea otrafunción que calcule el producto escalar recibiendo dos arrays
 que representan dos vectores de la misma longitud. Si las dimensiones de losvectores impiden que se pueda calcular el 
 producto escalar, se puedecompletar con 0s o se puede devolver un código Integer.MIN_VALUE queindique un error. */
public class ejercicio9 {
    
}
